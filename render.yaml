services:
  - type: web
    name: art-class-generator
    runtime: python
    plan: free
    region: oregon
    branch: main
    buildCommand: pip install -r requirements.txt
    startCommand: python app.py
    healthCheckPath: /
    envVars:
      - key: BUDDY_CLOUD_TOKEN
        sync: false
